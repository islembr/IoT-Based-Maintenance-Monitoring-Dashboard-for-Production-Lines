import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service'; import { cellColor, PERIODICITY_COLORS } from '../../shared/maintenance.util';
@Component({selector:'app-plan-hebdomadaire',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">SW1 — SW52</p><h1>Plan hebdomadaire</h1><p>Les 52 semaines du classeur, avec filtre par ligne et plage.</p></div></header><section class="card"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une ligne…"><select class="control" [(ngModel)]="quarter"><option value="all">Toute l'année</option><option value="1">SW1–SW13</option><option value="2">SW14–SW26</option><option value="3">SW27–SW39</option><option value="4">SW40–SW52</option></select><span class="count">{{filteredRows.length}} lignes · {{visibleWeeks.length}} semaines</span></div><div class="table-scroll plan"><table><thead><tr><th class="sticky">Ligne de production</th><th *ngFor="let week of visibleWeeks">{{week.label}}</th></tr></thead><tbody><tr *ngFor="let row of filteredRows"><td class="sticky"><strong>{{row.machine}}</strong></td><td *ngFor="let week of visibleWeeks" [style.background]="color(row.tasks[week.index])" [title]="row.tasks[week.index]||'Aucune tâche'"><span>{{row.tasks[week.index]||'—'}}</span></td></tr><tr class="status-row"><td class="sticky"><strong>Statut</strong></td><td *ngFor="let week of visibleWeeks" [class.leave]="isLeave(data.weeklyStatus[week.index])">{{isLeave(data.weeklyStatus[week.index])?'Congé':''}}</td></tr></tbody></table></div><div class="legend"><span *ngFor="let p of periodKeys"><i [style.background]="periodColors[p]"></i>{{p}}</span></div></section>`,styles:[`.plan{max-height:72vh}.plan table{width:max-content;min-width:100%}.plan td:not(.sticky),.plan th:not(.sticky){min-width:88px;text-align:center}.plan td:not(.sticky){font-size:.68rem;font-weight:700;color:#294531}.sticky{position:sticky;left:0;z-index:3;background:#fff;min-width:165px}.plan thead .sticky{z-index:4;background:#f2f7f3}.status-row td{background:#f8faf8;color:#718078}.status-row .leave{background:#d9dee0;color:#45515a}.legend{display:flex;gap:1rem;flex-wrap:wrap;margin-top:.9rem;font-size:.7rem;color:#64756a}.legend span{display:flex;align-items:center;gap:.35rem}.legend i{width:10px;height:10px;border-radius:3px}`]})
export class PlanHebdomadaireComponent{readonly data=inject(MaintenanceDataService);readonly periodKeys=['Mensuelle','Trimestrielle','Semestrielle','Annuelle'] as const;readonly periodColors=PERIODICITY_COLORS;query='';quarter='all';get filteredRows(){const q=this.query.toLowerCase();return this.data.weeklyPlan.filter(x=>x.machine.toLowerCase().includes(q));}get visibleWeeks(){const start=this.quarter==='all'?0:(Number(this.quarter)-1)*13;const end=this.quarter==='all'?52:start+13;return this.data.weeks.map((label,index)=>({label,index})).slice(start,end);}color(value:string|null){const hex=cellColor(value);return `${hex}44`;}isLeave(v:string|number|null){return String(v||'').toLowerCase().includes('cong');}}
