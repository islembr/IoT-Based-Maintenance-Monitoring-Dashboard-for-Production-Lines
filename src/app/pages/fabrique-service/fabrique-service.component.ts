import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service';
@Component({selector:'app-fabrique-service',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Inventaire équipements</p><h1>Parc du service innovation</h1><p>Machines fabriquées, normalisées par site et emplacement.</p></div></header><section class="card"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Machine, site, emplacement…"><select class="control" [(ngModel)]="site"><option value="all">Tous les sites</option><option *ngFor="let s of sites" [value]="s">{{s}}</option></select><select class="control" [(ngModel)]="type"><option value="all">Tous les types</option><option value="KTR">KTR</option><option value="MODAL">MODAL</option></select><span class="count">{{filtered.length}} machines</span></div><div class="table-scroll"><table><thead><tr><th>Machine</th><th>Type</th><th>Année</th><th>Emplacement</th><th>Site</th></tr></thead><tbody><tr *ngFor="let row of filtered"><td><strong>{{row.machine}}</strong></td><td>{{row.type}}</td><td>{{row.constructionYear}}</td><td>{{row.location||'Non renseigné'}}</td><td><span class="pill site">{{row.site}}</span></td></tr></tbody></table></div></section>`,styles:[`.site{background:#dcfce7;color:#166534}`]})
export class FabriqueServiceComponent{readonly data=inject(MaintenanceDataService);query='';site='all';type='all';get sites(){return [...new Set(this.data.factoryMachines.map(x=>x.site))].sort();}get filtered(){const q=this.query.toLowerCase();return this.data.factoryMachines.filter(x=>(this.site==='all'||x.site===this.site)&&(this.type==='all'||x.type===this.type)&&(`${x.machine} ${x.site} ${x.location}`.toLowerCase().includes(q)));}}
