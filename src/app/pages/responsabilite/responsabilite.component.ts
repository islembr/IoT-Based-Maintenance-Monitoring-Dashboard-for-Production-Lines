import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service';
@Component({selector:'app-responsabilite',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Périmètre maintenance</p><h1>Chaînes sous notre responsabilité</h1><p>Liste normalisée des équipements suivis par l'équipe.</p></div></header><section class="card"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une chaîne…"><select class="control" [(ngModel)]="year"><option value="all">Toutes les années</option><option *ngFor="let y of years" [value]="y">{{y}}</option></select><span class="count">{{filtered.length}} chaînes</span></div><div class="table-scroll"><table><thead><tr><th>Machine</th><th>Configuration</th><th>Année</th><th>Emplacement</th><th>Site</th></tr></thead><tbody><tr *ngFor="let row of filtered"><td><strong>{{row.machine}}</strong></td><td>{{row.type}}</td><td>{{row.constructionYear}}</td><td>{{row.location||'Non renseigné'}}</td><td><span class="pill site">{{row.site}}</span></td></tr></tbody></table></div></section>`,styles:[`.site{background:#dcfce7;color:#166534}`]})
export class ResponsabiliteComponent{readonly data=inject(MaintenanceDataService);query='';year='all';get years(){return [...new Set(this.data.responsibilityMachines.map(x=>x.constructionYear))].sort();}get filtered(){const q=this.query.toLowerCase();return this.data.responsibilityMachines.filter(x=>(this.year==='all'||x.constructionYear===Number(this.year))&&(`${x.machine} ${x.type} ${x.location}`.toLowerCase().includes(q)));}}
