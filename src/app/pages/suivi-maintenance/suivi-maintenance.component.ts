import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceStatus } from '../../models/maintenance.models'; import { MaintenanceDataService } from '../../services/maintenance-data.service'; import { DonutChartComponent, DonutSlice } from '../../shared/donut-chart.component'; import { formatDateFr, PERIODICITY_COLORS, statusColor } from '../../shared/maintenance.util';
const MONTHS=['Jan','Fév','Mar','Avr','Mai','Juin','Juil','Août','Sep','Oct','Nov','Déc']; const CYCLE=['Annuelle',null,null,'Trimestrielle',null,null,'Semestrielle',null,null,'Trimestrielle',null,null] as const;
@Component({selector:'app-suivi-maintenance',standalone:true,imports:[CommonModule,FormsModule,DonutChartComponent],template:`<header class="page-header"><div><p class="eyebrow">Plan annuel</p><h1>Suivi de maintenance</h1><p>Échéances et périodicités par ligne.</p></div></header><div class="summary"><section class="card chart-card"><h2>Répartition des statuts</h2><app-donut-chart [slices]="slices"/></section><section class="card"><h2>Statut par ligne</h2><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une ligne…"><select class="control" [(ngModel)]="status"><option value="all">Tous les statuts</option><option *ngFor="let s of statuses" [value]="s">{{s}}</option></select></div><div class="table-scroll status-table"><table><thead><tr><th>Ligne</th><th>Date</th><th>Statut</th></tr></thead><tbody><tr *ngFor="let row of filtered"><td><strong>{{row.machine}}</strong></td><td>{{formatDate(row.annualDate)}}</td><td><span class="pill status" [style.background]="color(row.status)">{{row.status}}</span></td></tr></tbody></table></div></section></div><section class="card annual"><div><h2>Plan de maintenance annuel</h2><p class="muted">Cycle de périodicité reproduit à partir du classeur.</p></div><div class="table-scroll"><table class="grid"><thead><tr><th class="sticky">Ligne</th><th *ngFor="let m of months">{{m}}</th></tr></thead><tbody><tr *ngFor="let row of data.tracking;let ri=index"><td class="sticky"><strong>{{row.machine}}</strong></td><td *ngFor="let m of months;let ci=index" [style.background]="cellColor(ri,ci)"></td></tr></tbody></table></div><div class="legend"><span *ngFor="let p of periodKeys"><i [style.background]="periodColors[p]"></i>{{p}}</span></div></section>`,styles:[`.summary{display:grid;grid-template-columns:minmax(300px,.8fr) 1.2fr;gap:1rem;margin-bottom:1rem}.chart-card{display:flex;flex-direction:column;justify-content:center}.status{color:#fff}.status-table{max-height:330px}.annual>div:first-child{display:flex;justify-content:space-between;align-items:center}.grid{min-width:760px}.grid th:not(.sticky),.grid td:not(.sticky){text-align:center;min-width:44px}.grid td:not(.sticky){height:30px;padding:0}.sticky{position:sticky;left:0;z-index:3;background:#fff;min-width:150px}.grid thead .sticky{z-index:4;background:#f2f7f3}.legend{display:flex;gap:1rem;flex-wrap:wrap;margin-top:.8rem;font-size:.7rem;color:#64756a}.legend span{display:flex;align-items:center;gap:.35rem}.legend i{width:10px;height:10px;border-radius:3px}@media(max-width:1000px){.summary{grid-template-columns:1fr}}` ]})
export class SuiviMaintenanceComponent{readonly data=inject(MaintenanceDataService);readonly months=MONTHS;readonly statuses:MaintenanceStatus[]=['Terminé','Ce mois','Bientôt'];readonly periodKeys=['Mensuelle','Trimestrielle','Semestrielle','Annuelle'] as const;readonly periodColors=PERIODICITY_COLORS;query='';status='all';formatDate=formatDateFr;color=statusColor;get slices():DonutSlice[]{return this.data.statusCounts.map(x=>({label:x.label,value:x.value,color:statusColor(x.label as MaintenanceStatus)}));}get filtered(){const q=this.query.toLowerCase();return this.data.tracking.filter(x=>(this.status==='all'||x.status===this.status)&&x.machine.toLowerCase().includes(q));}cellColor(row:number,col:number){const p=CYCLE[(row+col)%12];return p?PERIODICITY_COLORS[p]:'#e2e8e4';}}
