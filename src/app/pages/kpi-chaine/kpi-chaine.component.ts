import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service';
@Component({selector:'app-kpi-chaine',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Performance</p><h1>KPI par chaîne</h1><p>Indicateurs calculés dans le classeur source.</p></div></header><section class="metric-grid params"><div class="metric" *ngFor="let p of data.kpiParameters"><small>{{p.label}}</small><strong>{{p.value | number:'1.0-2'}} <em>{{p.unit}}</em></strong></div></section><section class="card"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une chaîne…"><select class="control" [(ngModel)]="failureFilter"><option value="all">Toutes les lignes</option><option value="with">Avec panne</option><option value="without">Sans panne</option></select><span class="count">{{filtered.length}} résultats</span></div><div class="table-scroll"><table><thead><tr><th>Chaîne</th><th>Pannes</th><th>Arrêt (h)</th><th>Fonctionnement (h)</th><th>MTTR (h)</th><th>MTBF (h)</th><th>Disponibilité</th></tr></thead><tbody><tr *ngFor="let r of filtered"><td><strong>{{r.machine}}</strong></td><td><span class="pill failures" [class.zero]="!r.failures">{{r.failures}}</span></td><td>{{r.downtimeHours|number:'1.0-2'}}</td><td>{{r.operatingHours|number:'1.0-1'}}</td><td>{{r.mttrHours|number:'1.0-2'}}</td><td>{{r.mtbfHours|number:'1.0-1'}}</td><td><div class="availability"><div><span [style.width.%]="r.availability*100"></span></div><strong>{{r.availability|percent:'1.2-2'}}</strong></div></td></tr></tbody></table></div></section>`,styles:[`.params{margin-bottom:1rem}.metric strong{font-size:1.15rem}.metric em{font-size:.7rem;font-style:normal;color:#718078}.failures{background:#fee2e2;color:#b91c1c}.failures.zero{background:#dcfce7;color:#166534}.availability{display:flex;align-items:center;gap:.55rem}.availability>div{width:90px;height:7px;border-radius:9px;background:#e8efe9;overflow:hidden}.availability span{display:block;height:100%;background:#22a447}.availability strong{font-size:.72rem}`]})
export class KpiChaineComponent{readonly data=inject(MaintenanceDataService);query='';failureFilter='all';get filtered(){const q=this.query.toLowerCase().trim();return this.data.kpis.filter(r=>(!q||r.machine.toLowerCase().includes(q))&&(this.failureFilter==='all'||(this.failureFilter==='with'?r.failures>0:r.failures===0)));}}
