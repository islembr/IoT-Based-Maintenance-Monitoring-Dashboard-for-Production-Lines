import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service'; import { formatDateFr } from '../../shared/maintenance.util';
@Component({selector:'app-insertion-intervention',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Historique en lecture seule</p><h1>Interventions non planifiées</h1><p>Registre extrait de la feuille « Insertion d'intervention ».</p></div></header><section class="metric-grid"><div class="metric"><small>Interventions</small><strong>{{data.interventions.length}}</strong></div><div class="metric"><small>Durée totale</small><strong>{{totalHours|number:'1.1-1'}} h</strong></div><div class="metric"><small>Lignes concernées</small><strong>{{machineOptions.length}}</strong></div><div class="metric"><small>Codes utilisés</small><strong>{{usedCodes}}</strong></div></section><section class="card log"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une intervention…"><select class="control" [(ngModel)]="machine"><option value="all">Toutes les lignes</option><option *ngFor="let m of machineOptions" [value]="m">{{m}}</option></select><select class="control" [(ngModel)]="month"><option value="all">Tous les mois</option><option *ngFor="let m of monthOptions" [value]="m">{{m}}</option></select><span class="count">{{filtered.length}} résultats</span></div><div class="table-scroll"><table><thead><tr><th>Ligne</th><th>Code</th><th>Description</th><th>Date</th><th>Mois</th><th>Durée</th></tr></thead><tbody><tr *ngFor="let item of filtered"><td><strong>{{item.machine}}</strong></td><td><span class="pill code" *ngIf="item.code;else noCode">{{item.code}}</span><ng-template #noCode><span class="muted">Non codé</span></ng-template></td><td>{{item.description}}</td><td>{{formatDate(item.date)}}</td><td>{{item.month}}</td><td>{{item.durationMinutes}} min <small>({{item.durationHours|number:'1.0-2'}} h)</small></td></tr></tbody></table></div></section><section class="card"><h2>Référentiel des codes correctifs</h2><div class="codes"><div *ngFor="let action of data.correctiveActions" [class.missing]="!action.documented"><strong>{{action.code}}</strong><span>{{action.description}}</span><small *ngIf="!action.documented">Non documenté</small></div></div></section>`,styles:[`.log{margin:1rem 0}.code{background:#dcfce7;color:#166534}.codes{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:.6rem}.codes>div{display:grid;grid-template-columns:45px 1fr;gap:.5rem;padding:.7rem;border:1px solid #e5ece6;border-radius:9px;font-size:.76rem}.codes strong{color:#157b35}.codes small{grid-column:2;color:#a16207}.codes .missing{background:#fffbeb;border-color:#fde68a}td small{color:#718078}`]})
export class InsertionInterventionComponent{readonly data=inject(MaintenanceDataService);query='';machine='all';month='all';formatDate=formatDateFr;get totalHours(){return this.data.interventions.reduce((s,x)=>s+x.durationHours,0);}get machineOptions(){return [...new Set(this.data.interventions.map(x=>x.machine))].sort();}get monthOptions(){return [...new Set(this.data.interventions.map(x=>x.month))].sort();}get usedCodes(){return new Set(this.data.interventions.map(x=>x.code).filter(Boolean)).size;}get filtered(){const q=this.query.toLowerCase();return this.data.interventions.filter(x=>(this.machine==='all'||x.machine===this.machine)&&(this.month==='all'||x.month===this.month)&&(`${x.machine} ${x.code} ${x.description}`.toLowerCase().includes(q)));}}
