import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { Periodicity } from '../../models/maintenance.models'; import { MaintenanceDataService } from '../../services/maintenance-data.service'; import { PERIODICITY_COLORS } from '../../shared/maintenance.util';
@Component({selector:'app-action-maintenance',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Référentiel</p><h1>Actions de maintenance</h1><p>Actions systématiques et codes correctifs du classeur.</p></div></header><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une action…"><select class="control" [(ngModel)]="period"><option value="all">Toutes les périodicités</option><option *ngFor="let p of periods" [value]="p">{{p}}</option></select></div><div class="actions-grid"><section class="card"><h2>Préventif <span>{{filteredPreventive.length}}</span></h2><div class="table-scroll"><table><thead><tr><th>Code</th><th>Action</th><th>Périodicité</th></tr></thead><tbody><tr *ngFor="let a of filteredPreventive"><td><strong>{{a.code}}</strong></td><td>{{a.description}}</td><td><span class="pill period" [style.background]="colors[a.periodicity]">{{a.periodicity}}</span></td></tr></tbody></table></div></section><section class="card"><h2>Correctif <span>{{filteredCorrective.length}}</span></h2><div class="table-scroll"><table><thead><tr><th>Code</th><th>Action</th></tr></thead><tbody><tr *ngFor="let a of filteredCorrective"><td><strong>{{a.code}}</strong></td><td [class.missing]="!a.documented">{{a.description}} <small *ngIf="!a.documented">À compléter dans Excel</small></td></tr></tbody></table></div></section></div>`,styles:[`.actions-grid{display:grid;grid-template-columns:1.4fr 1fr;gap:1rem}.card h2{display:flex;justify-content:space-between}.card h2 span{color:#718078;font-size:.72rem}.period{color:#fff}.missing{color:#946200;font-style:italic}.missing small{display:block;color:#b7791f;margin-top:.2rem}@media(max-width:1050px){.actions-grid{grid-template-columns:1fr}}` ]})
export class ActionMaintenanceComponent{readonly data=inject(MaintenanceDataService);readonly colors=PERIODICITY_COLORS;readonly periods:Periodicity[]=['Mensuelle','Trimestrielle','Semestrielle','Annuelle'];query='';period='all';get filteredPreventive(){const q=this.query.toLowerCase();return this.data.preventiveActions.filter(a=>(this.period==='all'||a.periodicity===this.period)&&(`${a.code} ${a.description}`.toLowerCase().includes(q)));}get filteredCorrective(){const q=this.query.toLowerCase();return this.data.correctiveActions.filter(a=>`${a.code} ${a.description}`.toLowerCase().includes(q));}}
