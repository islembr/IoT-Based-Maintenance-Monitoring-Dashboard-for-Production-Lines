import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { MaintenanceDataService } from '../../services/maintenance-data.service'; import { formatDateFr } from '../../shared/maintenance.util';
@Component({selector:'app-pieces-rechange',standalone:true,imports:[CommonModule,FormsModule],template:`<header class="page-header"><div><p class="eyebrow">Inventaire</p><h1>Pièces de rechange</h1><p>Quantités disponibles dans le classeur.</p></div></header><section class="metric-grid"><div class="metric"><small>Références listées</small><strong>{{data.spareParts.length}}</strong></div><div class="metric"><small>Quantité totale</small><strong>{{totalQuantity}}</strong></div><div class="metric"><small>Fiches incomplètes</small><strong>{{incomplete}}</strong></div></section><section class="card parts"><div class="toolbar"><input class="control" [(ngModel)]="query" placeholder="Rechercher une pièce…"><span class="count">{{filtered.length}} résultats</span></div><div class="table-scroll"><table><thead><tr><th>Pièce</th><th>Référence</th><th>Quantité</th><th>Date de rechange</th></tr></thead><tbody><tr *ngFor="let part of filtered"><td><strong>{{part.name}}</strong></td><td>{{part.reference||'Non renseignée'}}</td><td><span class="quantity">{{part.quantity}}</span></td><td>{{formatDate(part.replacementDate)}}</td></tr></tbody><tfoot><tr><td colspan="2">Total des quantités</td><td>{{totalQuantity}}</td><td>—</td></tr></tfoot></table></div><p class="note">Le total additionne la colonne Quantité, conformément à la logique attendue du classeur.</p></section>`,styles:[`.parts{margin-top:1rem}.quantity{font-weight:800;color:#166534}tfoot td{font-weight:800;background:#f2f7f3;color:#17351f}.note{margin:.8rem 0 0;color:#718078;font-size:.72rem}`]})
export class PiecesRechangeComponent{readonly data=inject(MaintenanceDataService);query='';formatDate=formatDateFr;get totalQuantity(){return this.data.spareParts.reduce((sum,p)=>sum+p.quantity,0);}get incomplete(){return this.data.spareParts.filter(p=>!p.reference||!p.replacementDate).length;}get filtered(){const q=this.query.toLowerCase();return this.data.spareParts.filter(p=>`${p.name} ${p.reference}`.toLowerCase().includes(q));}}
